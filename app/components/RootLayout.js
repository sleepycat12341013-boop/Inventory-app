<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
  <NavBar />

  <h1 className="text-xl font-bold px-4 py-3 border-b">在庫管理アプリ</h1>

  <main className="p-4">{children}</main>
</body>;
